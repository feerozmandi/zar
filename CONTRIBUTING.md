# راهنمای مشارکت در پروژه `zar` (پلتفرم Xennic)

این سند قوانین و گردش کار توسعه‌ی پروژه **Xennic** (مخزن `zar` شرکت سهامی خاص زر نور نیرو یکتا) را تعیین می‌کند. همه‌ی توسعه‌دهندگان، بازبینان و مدیران موظف به رعایت آن هستند.

---

## ۱. اصول کلی

1. `main` شاخه‌ی انتشار رسمی، عمومی و پایدار پروژه است؛ **هیچ کامیت مستقیمی روی آن مجاز نیست.**
2. `dev` شاخه‌ی یکپارچه‌سازی توسعه است؛ نتایج توسعه‌های تیمی پس از گذر از دروازه‌های کیفیت QA وارد آن می‌شود.
3. هر توسعه‌دهنده از `dev` شاخه‌ی اختصاصی می‌گیرد (`feat/*`, `fix/*`, `docs/*`) و مستقل در آن توسعه می‌دهد.
4. **قانون طلایی:** پیش از شروع هر تغییر، حتماً از شاخه‌ی والد بروزرسانی بگیرید (`git fetch origin && git pull origin dev`).
5. همه‌ی تغییرات از مسیر **Pull Request** و همراه با بازبینی (حداقل ۱ نفر) و پاس شدن ۱۰۰٪ چک‌های خودکار CI ادغام می‌شوند.

---

## ۲. گردش کار گام‌به‌گام

### ۲-۱. شروع کار روی یک تغییر

```bash
# ۱. همگام‌سازی کامل شاخه dev
git fetch origin
git checkout dev
git pull origin dev

# ۲. انشعاب شاخه اختصاصی از آخرین dev
git checkout -b feat/my-feature dev
# نمونه‌ها: feat/audit-ocr, fix/solar-roi-calc, docs/api-spec, chore/deps
```

### ۲-۲. حین توسعه

- کامیت‌های **کوچک و منسجم** بسازید (هر کامیت یک تغییر منطقی).
- از قرارداد پیام کامیت Conventional Commits (بخش ۳) پیروی کنید.
- فایل‌های حساس (`.env`، کلیدها، توکن‌ها و لاگ‌ها) هرگز کامیت نشوند.
- بهینه‌سازی حجم فایل‌ها و پرهیز از ذخیره فایل‌های باینری سنگین در گیت.

### ۲-۳. قبل از Pull Request

```bash
# همگام‌سازی مجدد با dev و حل تعارض‌های احتمالی
git fetch origin
git merge origin/dev        # یا: git rebase origin/dev

# اجرای کامل ماتریس آزمون‌ها به صورت محلی
pnpm lint
pnpm typecheck
pnpm test
pnpm build

# بررسی‌های نهایی گیت
git diff --check            # بررسی عدم وجود خطاهای whitespace
git status                  # بررسی تمیز بودن وضعیت فایل‌ها
```

### ۲-۴. ارسال PR به مقصد dev

```bash
git push -u origin feat/my-feature
# سپس در GitHub: ایجاد PR با پایه dev و قالب .github/PULL_REQUEST_TEMPLATE.md
```

1. عنوان PR: `feat(scope): توضیح تغییر`
2. توضیح کامل تغییرات، وابستگی‌ها و نحوه تست
3. گذر از تمام چک‌های دروازه کیفیت QA (تست، تایپ‌چک، امنیت، لینت)
4. حداقل ۱ بازبینی و تأیید از افراد مشخص‌شده در `CODEOWNERS`

---

## ۳. خط‌مشی بهینه‌سازی حجم و پالایش فایل‌های شاخه `main` (Lean Main & Production Release Policy)

برای تضمین سبکی، سرعت کلون و استقرار، و جلوگیری از افزایش بی‌رویه حجم مخزن در شاخه‌ی انتشار عمومی (`main`)، این خط‌مشی در آخرین مرحله‌ی هر چرخه توسعه هنگام انتقال به `main` لازم‌الاجرا است:

### ۳-۱. اهداف خط‌مشی
- **سبک ماندن شاخه اصلی (`main`):** شاخه `main` تنها باید حاوی فایل‌های ضروری اجرای پروداکشن، پکیج‌های نرم‌افزاری فعال، مستندات رسمی عمومی و اسکریپت‌های استقرار باشد.
- **جلوگیری از انباشت فایل‌های سنگین:** فایل‌های تحقیقاتی خام، یادداشت‌های پیش‌نویس موقت، فایل‌های باینری بزرگ (ویدیوها، دیتاست‌های آزمایشی، مدل‌های حجیم) نباید در `main` نگهداری شوند.

### ۳-۲. مانیفست اقلام مجاز در شاخه `main`

| دسته‌بندی | اقلام مجاز برای انتشار در `main` | اقلام ممنوعه یا نیازمند پالایش |
| :--- | :--- | :--- |
| **کدها و بسته‌ها** | `apps/api`, `apps/web`, `packages/*` | کدهای آزمایشی منسوخ، ماک‌های حجیم بدون استفاده |
| **زیرساخت و پیکربندی** | `docker/`, `pnpm-workspace.yaml`, `turbo.json`, `package.json`, `.env.example` | کانفیگ‌های تست محلی، فایل‌های شل موقت |
| **مستندات عمومی** | `README.md`, `CONTRIBUTING.md`, `SECURITY.md`, `LICENSE`, `GITHUB_SETUP_GUIDE.md` | پیش‌نویس‌های حل‌نشده، اسناد ناقص درون‌تیمی |
| **راهبری و CI** | `.github/rulesets/`, `.github/CODEOWNERS`, `ci/` | لاگ‌های اجرا، گزارش‌های موقت بیلد |
| **رسانه‌ها و تصاویر** | آیکون‌ها و دیاگرام‌های برداری بهینه (SVG / WebP < 500KB) | فایل‌های باینری، PDFهای حجیم یا تصاویر فشرده‌نشده بالای ۲MB |

### ۳-۳. فرآیند پالایش پیش از ساخت Release PR به `main`

پیش از ایجاد Pull Request از `dev` به `main`:
1. **بررسی حجم فایل‌های جدید:**
   ```bash
   # بررسی فایل‌های بزرگتر از ۱ مگابایت
   find . -not -path '*/.*' -not -path '*/node_modules/*' -size +1M
   ```
2. **بررسی و انتقال فایل‌های باینری سنگین:** فایل‌های باینری حجیم باید به جای کامیت در مخزن، در Object Storage (مانند S3 یا ArvanCloud) یا GitHub Releases ذخیره و به صورت لینک ارجاع داده شوند.
3. **پاکسازی فایل‌های موقت:** حذف هرگونه اسناد ناقص یا ابزارهای موقت که صرفاً برای آزمایش‌های محلی در `dev` ایجاد شده بودند.
4. **ایجاد PR با تگ نسخه:** پس از بازبینی و ادغام در `main`، برچسب نسخه SemVer (`vX.Y.Z`) ایجاد و پوش می‌شود.

---

## ۴. قرارداد پیام کامیت (Conventional Commits)

قالب استاندارد: `type(scope): توضیح`

| نوع | کاربرد |
|:---|:---|
| `feat` | ویژگی جدید در پلتفرم |
| `fix` | رفع باگ و خطا |
| `docs` | افزودن یا اصلاح مستندات |
| `style` | فرمت‌بندی و استایل (بدون تغییر منطق کد) |
| `refactor` | بازسازی ساختار کد بدون تغییر رفتار |
| `perf` | بهبود کارایی و سرعت اجرای برنامه |
| `test` | افزودن یا تصحیح تست‌های خودکار |
| `ci` | تغییرات پایپ‌لاین‌های CI/CD |
| `build` | وابستگی‌ها، پکیج‌ها و تنظیمات ساخت |
| `chore` | وظایف نگهداری، پیکربندی و راهبری |

**نمونه‌ها:**
```text
feat(audit): implement reactive penalty calculator for industrial bills
fix(solar): correct IRR calculation formula for Article 16
docs(governance): add lean main branch release policy
```

---

## ۵. معیار تکمیل (Definition of Done)

یک تسک یا ویژگی تنها زمانی «تکمیل‌شده» محسوب می‌شود که:

- [ ] با آخرین نسخه `dev` همگام شده و فاقد تعارض (Merge Conflict) باشد.
- [ ] تمامی تست‌های واحد و یکپارچه‌سازی (`pnpm test`) با موفقیت پاس شده باشند.
- [ ] بررسی تایپ‌ها (`pnpm typecheck`) و لینت (`pnpm lint`) بدون هشدار و خطا باشد.
- [ ] بیلد پروژه در محیط Turborepo (`pnpm build`) با موفقیت انجام شود.
- [ ] هیچ کلید دسترسی، سکرت یا فایل حساسی در کدها وجود نداشته باشد.
- [ ] الزامات **خط‌مشی پالایش فایل‌های main** رعایت شده و حجم بیهوده‌ای اضافه نگردد.
- [ ] توسط حداقل ۱ نفر از دارندگان مالکیت در `CODEOWNERS` بررسی و تأیید شود.

---

## ۶. نقش‌ها و دسترسی‌ها

| نقش | مسئولیت |
|:---|:---|
| توسعه‌دهنده (Developer) | پیاده‌سازی روی شاخه‌های کاری و ارسال PR به `dev` |
| بازبین / مسئول کیفیت (QA) | بازبینی کدها، نظارت بر دروازه‌های کیفیت و معماری |
| نگهدارنده ارشد (Maintainer / Owner) | ادغام به شاخه پایدار `main`، ممیزی حجم و فایل‌ها، برچسب‌گذاری و انتشار نسخه‌ها |

---

*جهت گزارش مسائل امنیتی به [SECURITY.md](SECURITY.md) مراجعه فرمایید.*
