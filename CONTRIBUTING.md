# راهنمای مشارکت در پروژه `zar`

این سند قوانین و گردش کار توسعه‌ی پروژه **Xennic** (مخزن `zar` شرکت زر نور نیرو یکتا) را تعیین می‌کند. همه‌ی توسعه‌دهندگان، بازبینان و مدیران موظف به رعایت آن هستند.

---

## ۱. اصول کلی

1. `main` آخرین نقطه توسعه، تست‌شده و ورژن‌بندی‌شده است؛ **هیچ کامیت مستقیمی روی آن مجاز نیست.**
2. `dev` شاخه‌ی یکپارچه‌سازی توسعه است؛ نتایج تأییدشده‌ی توسعه‌دهندگان فقط از طریق PR وارد آن می‌شود.
3. هر توسعه‌دهنده از `dev` شاخه‌ی اختصاصی می‌گیرد و مستقل در آن توسعه می‌دهد.
4. **قانون طلایی:** پیش از شروع هر تغییر، حتماً از شاخه‌ی والد (از `main` به `dev`، از `dev` به `feature/*`) بروزرسانی بگیرید؛ سپس توسعه را آغاز کنید.
5. همه‌ی تغییرات از مسیر **Pull Request** و همراه با بازبینی (حداقل ۱ نفر) ادغام می‌شوند.

---

## ۲. گردش کار گام‌به‌گام

### ۲-۱. شروع کار روی یک تغییر

```bash
# 1) همگام‌سازی کامل
git fetch origin
git checkout dev
git pull origin dev

# 2) انشعاب شاخه اختصاصی از آخرین dev
git checkout -b feature/<نام-کوتاه-ویژگی> dev
# نمونه‌ها: feature/audit-ocr ، fix/solar-roi-bug ، docs/landing-spec ، chore/deps
```

### ۲-۲. حین توسعه

- کامیت‌های **کوچک و منسجم** بسازید (هر کامیت یک تغییر منطقی).
- از قرارداد پیام کامیت (بخش ۳) پیروی کنید.
- فایل‌های حساس (`.env`، کلیدها، لاگ‌ها) هرگز کامیت نشوند.

### ۲-۳. قبل از Pull Request

```bash
# همگام‌سازی مجدد با dev و حل تعارض احتمالی
git fetch origin
git merge origin/dev        # یا: git rebase origin/dev

# بررسی‌های نهایی
git diff --check            # مشکلات whitespace
git status                  # بررسی فایل‌های مرحله‌بندی‌شده
grep -rniE "(api[_-]?key|secret|token|password)" --include=".env*" .   # مطمئن شوید سکرتی در مخزن نیست
```

### ۲-۴. ارسال PR

```bash
git push -u origin feature/<نام-ویژگی>
# سپس در GitHub: PR با پایه dev و قالب .github/PULL_REQUEST_TEMPLATE.md
```

1. عنوان PR: `type: توضیح کوتاه`
2. توضیح کامل تغییرات، دلیل و نحوه‌ی تست
3. حداقل ۱ بازبینی (Review) و تأیید
4. پس از سبز شدن چک‌ها (CI — در گام‌های بعدی) ادغام کنید

### ۲-۵. انتقال نهایی به main

پس از تست، بازبینی و رفع خطاها در `dev`:

```bash
# توسط Maintainer / Owner
git checkout dev
git pull origin dev
git checkout main
git pull origin main
git merge --no-ff dev        # ترجیحاً: PR با پایه main
git push origin main
git tag v0.1.0 && git push origin v0.1.0   # نسخه‌گذاری بعد از انتشار
```

---

## ۳. قرارداد پیام کامیت (Conventional Commits)

قالب: `type(scope): توضیح`

| نوع | کاربرد |
|:---|:---|
| `feat` | ویژگی جدید |
| `fix` | رفع خطا |
| `docs` | مستندات |
| `style` | فرمت/استایل (بدون تغییر منطق) |
| `refactor` | بازسازی بدون تغییر رفتار |
| `perf` | بهبود کارایی |
| `test` | افزودن/اصلاح تست |
| `ci` | تغییرات CI |
| `build` | وابستگی‌ها و بیلد |
| `chore` | کارهای متفرقه |

**مثال‌ها:**

```text
feat(audit): افزودن OCR قبض برق
fix(solar): اصلاح فرمول بازگشت سرمایه ماده ۱۲
docs: به‌روزرسانی معماری API
```

---

## ۴. معیار تکمیل (Definition of Done)

یک تغییر فقط وقتی «تمام» است که:

- [ ] از آخرین نسخه‌ی `dev` بروزرسانی شده باشد (بدون تعارض حل‌نشده)
- [ ] تمام تست‌ها و بررسی‌های خودکار (در صورت وجود) سبز باشند
- [ ] تغییرات برای کاربر مستند شده باشد
- [ ] هیچ سکرت یا فایل حساسی وارد مخزن نشده باشد
- [ ] حداقل ۱ بازبینی تأیید کند و PR ادغام شود

---

## ۵. قالب‌ها و قواعد

- Pull Request: قالب اجباری `.github/PULL_REQUEST_TEMPLATE.md`
- Issue: قالب‌های `.github/ISSUE_TEMPLATE/`
- فایل‌های مستندات در `docs/`؛ تصاویر و منابع فقط در `docs/assets/` با مسیر نسبی.

---

## ۶. نقش‌ها

| نقش | مسئولیت |
|:---|:---|
| توسعه‌دهنده | توسعه روی `feature/*` و PR به `dev` |
| بازبین / QA | بررسی کد، تست، تأیید کیفیت |
| Maintainer / Owner | ادغام نهایی به `main`، نسخه‌بندی و انتشار |

---

*برای گزارش مسائل امنیتی به `SECURITY.md` مراجعه کنید.*
