# سیاست امنیتی پروژه `zar`

## دامنه

این سیاست برای مخزن `zar` و همه‌ی اجزای آینده‌ی آن (کد، مستندات، پیکربندی CI/CD و اسناد) معتبر است.

## گزارش آسیب‌پذیری

- آسیب‌پذیری‌ها را **به‌صورت خصوصی** از طریق **GitHub Security Advisories** (مسیر: `Security → Report a vulnerability` در مخزن) گزارش کنید.
- **هرگز** آسیب‌پذیری را قبل از رفع، در Issue عمومی یا PR منتشر نکنید.
- در گزارش ذکر کنید: نسخه/شاخه متأثر، شرح مشکل، مراحل بازتولید و شدت (در صورت امکان).

## تعهد به پاسخ‌گویی

- تأیید دریافت گزارش: حداکثر **۷ روز کاری**
- بررسی و اعلام برنامه‌ی رفع: حداکثر **۳۰ روز**
- در صورت تأیید، اطلاع‌رسانی شفاف به گزارش‌دهنده

## قواعد سخت‌گیرانه (Enforced Rules)

1. هیچ فایل `.env`، کلید API (GitHub Models / OpenAI / Anthropic / Gemini و…)، توکن یا داده‌ی مشتری در مخزن کامیت نمی‌شود؛ فقط از GitHub Secrets/متغیرهای محیطی استفاده می‌شود.
2. اعتبارنامه‌های BYOK کاربران در زمان اجرا، با AES-256-GCM رمزنگاری و در جدول جداگانه نگهداری می‌شوند (طبق نوت ۳).
3. سرورها و سرویس‌های پروژه فقط روی پورت‌های لازم و با حداقل دسترسی (Least Privilege) اجرا می‌شوند.
4. وابستگی‌ها از منابع رسمی و نسخه‌های پین‌شده (Pinned Versions) نصب می‌شوند؛ اسکن امنیتی در CI (گام‌های بعدی) الزامی است.
5. دسترسی‌ها در GitHub حداقلی است؛ هیچ API دسترسی ادمین/سکرتی در ربات‌ها و CI ذخیره نمی‌شود.

## محیط گزارش‌دهی

- گزارش‌ها فقط از طریق کانال‌های رسمی شرکت و بخش Security Advisories گیت‌هاب پذیرفته می‌شود.
- این مخزن دارای فایل LICENSE اختصاصی است؛ هرگونه استفاده‌ی خارجی بدون مجوز رسمی ممنوع است.
